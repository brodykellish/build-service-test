# Simple Dockerfile for the versions example. Building this twice
# produces v1 and v2 — same content, different image tags. The example
# verifies both URLs appear in the image resource's `versions[]`.

FROM alpine:3.20
RUN echo "this is a versioned image" > /info.txt
CMD ["cat", "/info.txt"]
